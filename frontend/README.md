# MyMitra Frontend

React frontend for MyMitra. See the [root README](../README.md) for full project documentation.

## Dev

```bash
npm install
npm start        # http://localhost:3000
npm run build    # production build
```

Requires backend running at `http://localhost:8000`. Set `REACT_APP_API_BASE` in `.env` to override.
